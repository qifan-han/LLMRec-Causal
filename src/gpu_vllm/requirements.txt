# Requirements for history-shock GPU pipeline (AutoDL or any CUDA-enabled server)
#
# Install order matters:
#   1. Install vllm first — it brings its own torch and CUDA-compatible stack.
#   2. Do NOT separately install torch; vllm pins the correct version.
#   3. Install remaining packages after vllm:
#        pip install vllm
#        pip install -r requirements.txt
#
# Tested on: vLLM 0.17.0, torch 2.10.0, CUDA 12.8, Python 3.10+
# Model: Qwen/Qwen2.5-32B-Instruct-AWQ (public, no auth needed)

# --- Core inference (vllm already installed in step 1) ---
transformers>=4.40.0

# --- Numerics / data ---
numpy>=1.26
pandas>=2.1
scipy>=1.12
tqdm>=4.66

# --- GPT evaluation (step 2) ---
openai>=1.0
tenacity>=8.0
python-dotenv>=1.0

# --- Model download (huggingface_hub pulled in by transformers) ---
huggingface_hub>=0.22
